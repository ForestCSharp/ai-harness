/target

# Secrets — never commit these.
.env
.env.*
!.env.example
*.key
secrets/

tmp/

# Per-project harness state: saved sessions, and whatever else becomes
# session-scoped. Written under the working directory the harness runs against.
.ai_harness/

# macOS
.DS_Store
